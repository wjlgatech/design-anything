---
name: use-seamly2d
description: >
  Drive Seamly2D for design-anything work — Open-source parametric pattern-making CAD — the most active open pattern tool. Use when a design
  task routes to this tool (garment-design). GENERATED satellite skill:
  facts fetched 2026-09-07; treat as a pointer, not gospel.
kind: skill
license: MIT
runtimes: [claude-code, codex, hermes]
generated: true
---

# use-seamly2d — satellite of FashionFreedom/Seamly2D

GENERATED by `scripts/satellites.py build` from `data/satellites.yml` —
edit the data or `KNOWLEDGE.md`, never this file (drift-gated).

> **STALE**: digest pinned to `dacc5600e29f` but upstream HEAD is `c24b74c75c14` — verify against the live repo before relying on details.

## Verified facts (as of 2026-09-07)

| Fact | Value |
|---|---|
| Repo | https://github.com/FashionFreedom/Seamly2D |
| License | GPL-3.0 |
| Stars | 958 |
| Last push | 2026-09-07 |
| Latest release | v2026.9.7.144 |
| HEAD | `c24b74c75c14` |
| Digest status | STALE |

## How to use it

1. Read `KNOWLEDGE.md` in this folder — the SHA-pinned deep digest (architecture, how to drive it, gotchas, verify command).
2. **Supply-chain rules (non-negotiable):** this is external code — read before
   running, prefer ephemeral use over persistent install, human-gate anything
   that executes. Popularity (958 stars) is not a safety proof.
3. Route outputs back through the matching design-anything gate — a satellite
   generates; only our gates say "ready".

## Verification (eval-with-teeth)

Facts above are verifiable against the live API:
`gh api repos/FashionFreedom/Seamly2D --jq .stargazers_count`. The digest's claims are
pinned to `dacc5600e29f`; if status is STALE, re-verify before
relying on internals. Success of any run is judged by OUR gate's exit 0.

## Honest edges

A satellite is a pointer + pinned digest, never a vendored copy: upstream can
change, relicense, or die — the weekly freshness sync measures that instead of
promising otherwise.
