---
name: use-spatiallm
description: >
  Drive SpatialLM for design-anything work — LLM for structured indoor modeling — point cloud/scene to walls, doors, and furniture layout as structured output. Use when a design
  task routes to this tool (interior-design). GENERATED satellite skill:
  facts fetched 2026-07-19; treat as a pointer, not gospel.
kind: skill
license: MIT
runtimes: [claude-code, codex, hermes]
generated: true
---

# use-spatiallm — satellite of manycore-research/SpatialLM

GENERATED by `scripts/satellites.py build` from `data/satellites.yml` —
edit the data or `KNOWLEDGE.md`, never this file (drift-gated).

## Verified facts (as of 2026-07-19)

| Fact | Value |
|---|---|
| Repo | https://github.com/manycore-research/SpatialLM |
| License | NOASSERTION |
| Stars | 4,615 |
| Last push | 2026-06-26 |
| Latest release | v0.1.1 |
| HEAD | `8913c44d84a4` |
| Digest status | fresh |

## How to use it

1. Read `KNOWLEDGE.md` in this folder — the SHA-pinned deep digest (architecture, how to drive it, gotchas, verify command).
2. **Supply-chain rules (non-negotiable):** this is external code — read before
   running, prefer ephemeral use over persistent install, human-gate anything
   that executes. Popularity (4,615 stars) is not a safety proof.
3. Route outputs back through the matching design-anything gate — a satellite
   generates; only our gates say "ready".

## Verification (eval-with-teeth)

Facts above are verifiable against the live API:
`gh api repos/manycore-research/SpatialLM --jq .stargazers_count`. The digest's claims are
pinned to `8913c44d84a4`; if status is STALE, re-verify before
relying on internals. Success of any run is judged by OUR gate's exit 0.

## Honest edges

A satellite is a pointer + pinned digest, never a vendored copy: upstream can
change, relicense, or die — the weekly freshness sync measures that instead of
promising otherwise.
